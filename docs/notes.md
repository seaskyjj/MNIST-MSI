# MNIST-MSI Notes

这份笔记整理了当前项目实现中涉及的主要知识点，包括整体代码逻辑、四个模型的实现方式、超参数搜索、PCA、数据增强、学习率调度和 early stopping。

## 1. 项目整体逻辑

当前项目的主实现集中在 `mnist_digit_recognition.ipynb`，整体流程可以概括为：

1. 加载 MNIST 数据集。
2. 将像素值归一化到 `[0, 1]`。
3. 将数据划分为训练集和测试集。
4. 在传统机器学习模型上先做超参数搜索，再用完整训练集重训最优模型。
5. 在神经网络模型上划分训练/验证集，用于学习率调度和 early stopping。
6. 统一输出测试集准确率、分类报告、混淆矩阵、训练时间和预测时间。
7. 补充 PCA / t-SNE 可视化以及最终的模型对比总结。

项目中实际比较的 4 个模型是：

- KNN
- Logistic Regression
- MLP
- CNN

其中 README 里有时把 MLP 和 CNN 一起归类为 Neural Network，但实现上它们是两个独立模型。

## 2. 训练、验证、测试的职责

### 训练集

用于拟合模型参数。

### 验证集

主要用于：

- 神经网络训练过程中监控效果
- early stopping
- 学习率调度

### 测试集

只在所有模型训练和选择完成后用于最终评估，不参与模型选择和超参数搜索。

这是避免数据泄露的基本前提。

## 3. KNN 的实现逻辑

KNN 当前实现为：

- `Pipeline(PCA + KNeighborsClassifier)`

它的核心思想是：

- 先用 PCA 降维，减少特征维数和距离计算成本
- 再用 KNN 基于邻居投票做分类

### 当前搜索的超参数

- `pca__n_components = [30, 50, 100]`
- `knn__n_neighbors = [1, 3, 5, 7, 9]`
- `knn__weights = ['uniform', 'distance']`
- `knn__metric = ['euclidean', 'manhattan']`

### 参数解释

#### `n_neighbors`

表示投票时使用多少个最近邻。

- 小 `k` 更灵活，但对噪声敏感
- 大 `k` 更平滑，但可能损失局部判别能力

#### `weights`

KNN 投票时邻居的权重策略：

- `uniform`：每个邻居权重相同
- `distance`：距离越近，权重越大

`distance` 往往在边界区域更稳，因为更近的样本更可信。

#### `metric`

距离计算方式：

- `euclidean`：欧氏距离
- `manhattan`：曼哈顿距离

不同距离度量可能改变最近邻结构。

## 4. Logistic Regression 的实现逻辑

当前 Logistic Regression 实现为独立的：

- `Pipeline(PCA + LogisticRegression)`

它不再复用 KNN 的 PCA 结果，而是有自己独立的 PCA 搜索空间。

### 当前搜索的超参数

- `pca__n_components = [30, 50, 100, 150]`
- `lr__C = [0.01, 0.1, 1.0, 10.0]`
- `lr__solver = ['lbfgs', 'saga']`
- `lr__penalty = ['l2']`

其中 `max_iter=2000` 固定。

### 参数解释

#### `C`

这是正则化强度的倒数：

- `C` 越小，正则化越强
- `C` 越大，正则化越弱

强正则可以防过拟合，但也可能欠拟合。

#### `solver`

这是 Logistic Regression 的优化器。

- `lbfgs`：常用、稳定，适合稠密特征和多分类
- `saga`：随机优化风格，更适合大规模问题，也支持更灵活的正则形式

在 MNIST 这种稠密输入上，`lbfgs` 通常是比较自然的强基线；搜索 `saga` 是为了验证它是否在这个任务上更有优势。

#### `penalty='l2'`

表示使用 L2 正则化，目的是抑制过拟合、稳定参数。

## 5. 为什么 KNN / LR 要用 Pipeline

这里最关键的原因是避免 PCA 数据泄露。

如果先在整份数据上做 PCA，再把变换后的结果送进交叉验证，那么每个验证折都会“提前看到”整份数据拟合出的 PCA 基底，这会让 CV 分数偏乐观。

把 PCA 放进 `Pipeline` 后：

- 每个 fold 都只在该 fold 的训练部分拟合 PCA
- 再把同一个 PCA 变换应用到该 fold 的验证部分

这是更标准、更安全的做法。

## 6. `GridSearchCV` 是怎么工作的

`GridSearchCV` 的作用是自动遍历一组超参数组合，并通过交叉验证比较它们的效果。

当前项目中判断标准是：

- `scoring='accuracy'`

也就是每组参数用交叉验证得到平均准确率，谁最高就选谁。

### 工作流程

以一组超参数为例：

1. 把训练数据分成 `K` 折。
2. 轮流使用 `K-1` 折训练，1 折验证。
3. 得到这一组参数在 `K` 折上的多个验证分数。
4. 对这些分数取平均。
5. 所有参数组合比较后，挑平均分最高的那组。

### `GridSearchCV` 是否真的训练模型

会，而且会训练很多次。

每个参数组合、每个 fold 都会重新拟合一次模型。

### `GridSearchCV` 是否会产生最终模型

会，默认 `refit=True`。

这意味着在找到最佳超参数后，它会再用传入的整份搜索数据重新训练一个最佳模型，并放在：

- `best_estimator_`

里。

### 为什么当前项目还会再手动训练一次

因为当前 KNN / LR 的超参数搜索是在训练集子集 `X_sub, y_sub` 上做的。

所以：

- `GridSearchCV` 最终 refit 的也是子集上的最佳模型
- 为了让最终模型充分利用完整训练集，还需要再在完整 `X_train, y_train` 上重训一次

如果一开始就是用完整训练集做 `GridSearchCV.fit(X_train, y_train)`，那通常可以直接使用 `best_estimator_`，不一定需要再手写一次训练。

## 7. KNN / LR 的搜索子集是否合理

当前做法是：

- 先从训练集里抽一个固定大小的子集
- 在这个子集上做 `GridSearchCV`
- 最后在完整训练集上重训最佳配置

这是一种常见的工程折中，尤其当搜索成本较高时。

### 这样做的优点

- 搜索速度更快
- 在大数据上更实用

### 需要满足的前提

- 子集必须只来自训练集，不能碰测试集
- 预处理必须放进 `Pipeline`

只要满足这两点，就不会构成测试集泄露。

### 子集是否需要为 KNN 和 LR 各自单独抽一份

通常不需要。

如果两个模型都用同一份固定子集，反而更公平，因为减少了“抽样不同”带来的额外波动。

## 8. 3 折 CV 是否够用

当前 `GridSearchCV` 用的是 3 折交叉验证。

这对本项目来说是合理的基线，不算太少，尤其考虑到搜索成本。

### 折数更高会发生什么

- 折数越高，验证估计通常越稳定
- 但训练次数也会明显增加

例如：

- 3 折更快，适合粗搜索
- 5 折更稳，但更慢
- 更高折数收益递减

### 是否 5 折一定更好

不一定。

5 折可能让超参数选择更稳定，但不保证一定选到“更好的”组合。它提升的是评估可靠性，不是必然提升最终精度。

### 实践建议

- KNN 继续用 3 折是可以接受的
- LR 如果希望更稳，可以尝试 5 折
- 更实用的方案是先 3 折全量搜索，再对前几组最优参数做 5 折复核

## 9. PCA 的 `n_components` 应该怎么选

PCA 的主成分数有两种常见思路。

### 按解释方差阈值选

例如：

- 保留 95% 信息量

可以直接写成：

```python
PCA(n_components=0.95)
```

这表示保留能解释总方差 95% 的最少主成分数。

### 把 `n_components` 当超参数搜索

例如：

- `[30, 50, 100, 150]`

然后通过分类准确率决定哪个好。

### 哪个更适合当前项目

如果目标是：

- 信息保留、压缩、可解释性：按方差阈值合理
- 下游分类效果：把 `n_components` 当超参数搜索更合理

因为 PCA 是无监督的，它保留的是高方差方向，不一定就是对分类最有用的方向。

### 折中方案

也可以搜索：

- `pca__n_components = [0.90, 0.95, 0.99]`

这样既体现“保留多少信息”，又让最终标准落到分类表现上。

## 10. MLP 的结构与实现依据

当前 MLP 结构为：

- `784 -> 512 -> 256 -> 128 -> 10`

每层之间使用：

- `BatchNorm`
- `ReLU`
- `Dropout`

其中 dropout 比例约为：

- `0.3 / 0.3 / 0.2`

### 这样设计的依据

- MNIST 输入维度不大，扁平化后用多层全连接可以学到有效的非线性边界
- `BatchNorm` 有助于训练稳定和收敛
- `Dropout` 有助于控制过拟合

这是一种典型的“课程项目级强 MLP”写法，复杂度适中，效果通常不错。

## 11. CNN 的结构与实现依据

当前 CNN 是一个小型卷积网络，整体结构接近轻量级的 VGG 风格：

1. 两层 `Conv + BN + ReLU`
2. `MaxPool + Dropout`
3. 再来一组更高通道数的卷积块
4. 展平后接全连接分类头

大致结构是：

```text
Input(1x28x28)
-> Conv(32) -> BN -> ReLU -> Conv(32) -> BN -> ReLU -> MaxPool -> Dropout
-> Conv(64) -> BN -> ReLU -> Conv(64) -> BN -> ReLU -> MaxPool -> Dropout
-> Flatten -> Linear(256) -> BN -> ReLU -> Dropout -> Linear(10)
```

### 为什么这样设计

- MNIST 是图像任务，卷积比 MLP 更擅长提取局部笔画和空间结构
- 图像只有 `28x28`，不需要很深的网络也能取得很高精度
- 这个结构已经足以达到接近饱和的性能

## 12. 是否换成浅层 ResNet 会更好

可能会略好，也可能更稳，但未必明显更好。

原因是：

- 当前 CNN 已经能在 MNIST 上达到很高精度
- ResNet 的优势在更深网络和更复杂数据集上更明显
- 对 `28x28` 灰度图，如果直接套标准 ResNet 结构，往往过重

### 结论

- 如果目标是课程项目、结构清晰、易解释：当前 CNN 很合适
- 如果目标是进一步榨取最后一点精度：可以尝试轻量级浅层 ResNet

如果以后要尝试，建议用适配 MNIST 的轻量变体，而不是直接照搬标准 ResNet-18 的 stem。

## 13. CNN 的 Data Augmentation 细节

当前 CNN 的增强是逐样本执行的，定义在自定义 `AugmentedDataset.__getitem__` 中，而不是整 batch 一起增强。

### 具体增强内容

- `RandomRotation(degrees=10)`：随机旋转 `±10°`
- `RandomAffine(degrees=0, translate=(0.1, 0.1))`：随机平移，横向和纵向各最多 `10%`

### 这样写的原因

如果对整批张量一次性应用随机变换，可能会导致一个 batch 共享同一组随机参数，增强多样性不足。

放在 `Dataset.__getitem__` 里后，每张图会独立采样自己的旋转和平移参数。

### 注意

- 这套增强只用于 CNN 的训练集
- 验证集和测试集不做增强

## 14. MLP 的数据增强

MLP 没有使用图像几何变换，而是在训练时对输入直接加高斯噪声：

- `std = 0.05`

并且会把结果裁剪回 `[0, 1]`。

这种增强方式更适合扁平向量输入，不依赖显式的二维图像结构。

## 15. `ReduceLROnPlateau` 是什么

`ReduceLROnPlateau` 的意思是：

- 当监控指标进入平台期时，自动降低学习率

当前项目里参数大致是：

- `patience=3`
- `factor=0.5`

也就是：

- 如果监控指标连续 3 个 epoch 没有明显改善
- 就把学习率乘以 `0.5`

例如：

- `0.001 -> 0.0005 -> 0.00025`

### 它的作用

- 前期用较大学习率快速下降
- 后期进入平台期时减小步长做细调

## 16. 当前 Early Stopping 的工作方式

当前 early stopping 的核心逻辑是：

1. 每个 epoch 都计算一次 `val_acc`
2. 如果当前 `val_acc` 高于历史最佳 `best_val_acc`
   - 更新最佳值
   - 保存最佳模型权重
   - 清空 patience 计数器
3. 如果没有超过历史最佳值
   - patience 计数器加一
4. 当连续若干个 epoch 都没有刷新最佳值时，停止训练
5. 训练结束后恢复最佳权重

### 当前优点

- 会恢复最佳验证精度对应的模型，而不是最后一个 epoch
- 对普通短期波动并不敏感，不会因为一次下降就立即停掉

### 当前局限

- 判断条件相对简单，只看是否超过 `best_val_acc`
- 没有 `min_delta`
- `scheduler` 和 `early stopping` 监控指标如果不一致，会让逻辑稍微分裂
- 如果模型在长平台期之后才出现更好的结果，过短的 `patience` 可能会提前停掉

## 17. 你担心的“局部抖动后未来可能更好”是否成立

这个担心是合理的。

需要区分两件事：

- 当前写法并不是“只比较一次最佳值”
- 而是每个 epoch 都和历史最佳比较

所以如果未来更好的结果出现在 `patience` 窗口之内，它不会 miss。

真正可能 miss 的情况是：

- 模型需要经历一个比较长的平台期
- 然后才会出现新的更高峰值

这时如果 `patience` 太小，就会提前停。

## 18. 如果更关心最终精度，early stopping 应该怎么设

如果目标是最终精度优先，而不是尽快停止训练，我建议把几个概念拆开：

- `use_early_stopping`
- `restore_best_weights`
- `monitor`
- `patience`
- `min_delta`
- `min_epochs`

### 为什么要拆开

即使关闭 early stopping，也仍然可以保留：

- 保存最佳验证精度权重
- 最后恢复最佳权重

这样模型可以训练满固定 epoch，但最终仍然使用最佳 checkpoint。

### 精度优先的推荐思路

如果开启 early stopping，建议：

- 监控 `val_acc`
- 使用 `mode='max'`
- 设置 `min_delta = 0.0005` 或 `0.001`
- 设置 `patience = 8 ~ 12`
- 设置 `min_epochs = 5`
- `restore_best_weights = True`

### 如果更担心提前停掉

可以采用更保守的策略：

- 保留 `ReduceLROnPlateau`
- 关闭 early stopping
- 固定训练满 `20 ~ 30` 个 epoch
- 最后恢复最佳 `val_acc` 权重

这通常更不容易错过后期的小幅提升。

## 19. `val_acc` 和 `val_loss` 监控哪个更合适

如果目标是最终分类精度优先，那么：

- 保存最佳模型时用 `val_acc` 是合理的

但训练控制层面仍有两种思路：

### 方案 A：全部监控 `val_acc`

优点：

- 目标一致，直接围绕最终精度优化

缺点：

- `val_acc` 变化通常更跳、更不平滑

### 方案 B：scheduler 看 `val_loss`，checkpoint 看 `val_acc`

优点：

- `val_loss` 更平滑，适合调学习率
- `val_acc` 更贴近最终任务目标

缺点：

- 训练控制逻辑有两个监控信号，理解上稍复杂

### 实践建议

如果是这个项目，我更建议：

- `ReduceLROnPlateau` 继续监控 `val_loss`
- 最佳模型保存和恢复继续用 `val_acc`
- early stopping 若保留，则放宽 patience，并加上 `min_delta`

这样通常比“只靠短 patience 的 `best_val_acc`”更稳。

## 20. 对当前项目比较合理的默认策略

如果以课程项目和实验稳定性为目标，较推荐的默认配置是：

- KNN / LR 继续使用 `Pipeline + GridSearchCV`
- 搜索先在训练子集上进行，最终再用完整训练集重训
- KNN 用 3 折 CV
- LR 可根据时间决定是否升到 5 折
- CNN 使用当前的小型卷积结构和逐样本增强
- 保留 `ReduceLROnPlateau`
- 保留最佳权重恢复

关于 early stopping，有两种都合理：

### 方案 1：更保守、更追求最终精度

- 关闭 early stopping
- 固定训练满 `20 ~ 30` 个 epoch
- 最后恢复最佳 `val_acc` 权重

### 方案 2：更节省时间

- 开启 early stopping
- 监控 `val_acc`
- 加 `min_delta`
- 把 `patience` 放宽到 `8 ~ 12`
- 保留 `restore_best_weights`

## 21. 一句话总结

这个项目当前最核心的工程思想是：

- 用 `Pipeline` 避免传统模型预处理泄露
- 用 `GridSearchCV` 系统化搜索超参数
- 用轻量但足够强的 MLP / CNN 结构处理 MNIST
- 用逐样本数据增强、学习率调度和最佳权重恢复来稳定神经网络训练

如果后续要继续改进，优先级通常是：

1. 把训练控制参数做成可配置项
2. 提高实验可复现性
3. 再考虑更复杂的网络结构
